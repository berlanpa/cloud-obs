# Redis
redis>=5.0.0

# LiveKit
livekit>=0.10.0
livekit-api>=0.5.0

# Core utilities
numpy>=1.24.0
pillow>=10.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
loguru>=0.7.0

# Audio/Video processing (for frame conversion)
# Use av 10.x for better ARM64 compatibility
av>=10.0.0

# ============================================
# LOCAL AI MODELS (CPU-optimized)
# ============================================

# YOLO11 object detection
ultralytics>=8.1.0
# For CPU: PyTorch will be installed automatically
torch>=2.0.0
torchvision>=0.15.0

# LLaVA vision-language model
transformers>=4.35.0
accelerate>=0.25.0
bitsandbytes>=0.41.0  # For 4-bit quantization
sentencepiece>=0.1.99
protobuf>=3.20.0

# Faster-Whisper speech recognition
faster-whisper>=0.10.0
# Uses CTranslate2 which works on CPU

# ByteTrack object tracking
scipy>=1.10.0
filterpy>=1.4.5  # For Kalman filters
lap>=0.4.0  # Linear assignment

# XGBoost (optional, for ML-based ranking)
xgboost>=2.0.0

# Image processing
opencv-python-headless>=4.8.0  # Headless version for servers
scikit-image>=0.21.0
