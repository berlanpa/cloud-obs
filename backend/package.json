{
  "name": "ai-obs",
  "version": "1.0.0",
  "description": "AI-powered auto-director with real-time camera switching, VLM analysis, and intelligent narration",
  "private": true,
  "workspaces": [
    "services/*",
    "web-obs",
    "shared/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:build": "docker-compose build"
  },
  "devDependencies": {
    "turbo": "^1.10.16",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
